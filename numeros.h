#ifndef numeros_h      // Se numeros_h ainda não foi definido
#define numeros_h      // Define numeros_h, para evitar inclusão múltipla

int num_par(int a);
int num_primo(int a, int b);
int fat(int a);
float maior(float a, float b);
int pot(int a, int b);

#endif                 // Fim da proteção de inclusão

